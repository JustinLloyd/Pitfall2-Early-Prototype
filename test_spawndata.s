
test_spawn_data::
  DB $00             ;; spawn type
  DW $0008             ;; spawn x pos
  DW $00A0             ;; spawn y pos
  DB $00             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $01             ;; spawn type
  DW $00A8             ;; spawn x pos
  DW $0060             ;; spawn y pos
  DB $01             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $02             ;; spawn type
  DW $0168             ;; spawn x pos
  DW $00A0             ;; spawn y pos
  DB $02             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $00             ;; spawn type
  DW $00C0             ;; spawn x pos
  DW $0148             ;; spawn y pos
  DB $03             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $00             ;; spawn type
  DW $0120             ;; spawn x pos
  DW $0118             ;; spawn y pos
  DB $04             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $01             ;; spawn type
  DW $0110             ;; spawn x pos
  DW $0008             ;; spawn y pos
  DB $05             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

  DB $80             ;; spawn type
  DW $0000             ;; spawn x pos
  DW $0000             ;; spawn y pos
  DB $80             ;; spawn path number
  DB $00             ;; spawn node index
  DB $00             ;; spawn start direction

